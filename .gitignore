# Reference clones of other projects (Mylar3, Sonarr) — never committed
.reference/

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.coverage
htmlcov/
.mypy_cache/
.ruff_cache/

# Node / frontend
node_modules/
dist/
build/
*.tsbuildinfo

# Runtime data
*.db
*.sqlite3
config/
data/
logs/

# The user's real comic library — test fixture, never committed
sample-comics/
.DS_Store

# Comment-hygiene local denylist (FRG-PROC-023): operator-specific literals
# tools/comment_check.py should flag — real collection titles, rig hostnames.
# Never committed; the tool skips that pass when it is absent.
tools/comment_check_local_denylist.txt

# Secrets — API keys live in env/.env, never in the repo
.env
.env.*
!.env.example

# OS / editor
.DS_Store
.idea/
.vscode/
.claude/worktrees/

# Design handoffs stay out of the repository (owner direction 2026-07-10;
# see KA-001 — a design export introduced the exposed-credential class).
# Guarded anywhere in the tree, and the .dc.html export type itself.
docs/research/*.zip
design_handoff_*/
design_handoff_*.zip
*.dc.html

# design-sync working state (durable set in .design-sync/ IS committed)
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# Built public site (site/build.py output; deployed by .github/workflows/pages.yml)
_site/
