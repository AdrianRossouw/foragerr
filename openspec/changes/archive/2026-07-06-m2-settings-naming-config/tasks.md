# Tasks: m2-settings-naming-config

- [ ] Delta specs drafted (pp: PP-012/013, ui: UI-012, api: API-013, dep: DEP-004)
      + design.md
- [ ] Area 1 (backend naming/preview + recycle bin): preview seam in renamer,
      recycle-bin replacing quarantine (with dir migration), config settings +
      schema version + forward migration (DEP-004), config resource endpoints
      (API-013) — Opus worktree agent
- [ ] Area 2 (frontend settings screen): naming/media-management screen with
      live preview (UI-012) — Opus worktree agent, after area 1 interfaces land
- [ ] Gate: 9-angle review fan-out + Codex, fixes, suites green (backend,
      frontend, e2e), security-docs check (FRG-PROC-006 — expected no new rows),
      manual sync (user/import.md, admin/configuration.md), registry flip (5),
      trace/soup exit 0, archive, --no-ff merge, tag v0.2.0, push (if secret set)
