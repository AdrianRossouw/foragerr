# library-sorts — tasks

- [x] 1.1 uiStore: LibrarySortKey + persistence whitelist + rehydration
      guard gain 'size' and 'latest'
- [x] 1.2 LibraryIndex: comparators (size_on_disk desc; last release
      date desc, nulls last; both tiebreak title) + two CheckRows
- [x] 1.3 Vitest: order correctness incl. null dates, persistence,
      menu-disabled-while-grouped unchanged; FRG-UI-003 in names
- [x] 1.4 Manual sort-list mention if enumerated; suites green; tools 0
