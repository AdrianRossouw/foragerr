# library-sorts — tasks

- [ ] 1.1 uiStore: LibrarySortKey + persistence whitelist + rehydration
      guard gain 'size' and 'latest'
- [ ] 1.2 LibraryIndex: comparators (size_on_disk desc; last release
      date desc, nulls last; both tiebreak title) + two CheckRows
- [ ] 1.3 Vitest: order correctness incl. null dates, persistence,
      menu-disabled-while-grouped unchanged; FRG-UI-003 in names
- [ ] 1.4 Manual sort-list mention if enumerated; suites green; tools 0
