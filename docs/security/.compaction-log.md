# risk-register.md compaction log (working artifact)

Compaction of the Decision column into `Status | Current mitigation`. One line per risk:
`RISK-NNN: <old final status marker(s)> -> <new Status cell>`.

RISK-001: "implemented as designed / impossible by construction (m1-ui-opds-deploy)" -> Mitigated (m1-ui-opds-deploy)
RISK-002: "implemented (m1-ui-opds-deploy)" -> Mitigated (m1-ui-opds-deploy)
RISK-003: "M8 status (m8-keys-opds): FRG-AUTH-005 implemented; independence residual closed" (after m8-auth-core perimeter) -> Mitigated (m8-auth-core; residual closed m8-keys-opds)
RISK-004: "Mitigate ->" only (no status marker in row) -> Mitigated [FLAGGED]
RISK-005: "M3 status (m3-opds-page-streaming): CLOSED — mitigation realised" + accepted residual (zipfile ZipInfo) -> Mitigated (m3-opds-page-streaming) [residual preserved]
RISK-006: "implemented (m1-downloads)" -> Mitigated (m1-downloads)
RISK-007: "implemented (m1-downloads)" -> Mitigated (m1-downloads)
RISK-008: "stays OPEN — FRG-DDL-015 backlog B, unimplemented; Not claimed as mitigated" -> Open (target: backlog B) [FLAGGED: contradicts Counts' Mitigate span]
RISK-009: "Mitigate ->" only (no status marker in row) -> Mitigated [FLAGGED]
RISK-010: "M3 status: cover-extraction now LIVE and routed; all arms routed through one utility" + accepted RAR M1 residual -> Mitigated (m1-import-pipeline; extended m2-manual-import, m3-opds-page-streaming) [residual preserved]
RISK-011: "M5 status (m5-creator-suggestions): further CV ingest arm covered by shared sanitizer" -> Mitigated (m1-library-metadata; extended m4-add-new, m5)
RISK-012: "implemented (m1-foundation)" -> Mitigated (m1-foundation)
RISK-013: "M6 status (m6-keystore): at-rest encryption implemented; last at-rest gap closed" -> Mitigated (m1-foundation; at-rest closed m6-keystore)
RISK-014: "M2 status: CR/LF log-forging CLOSED for request-sourced fields; DDL scraped-text arm UNCHANGED and remains OPEN" -> Mitigated (m1-library-metadata; request-sourced log-forging closed m2-hardening-performance) [DDL-arm open residual preserved]
RISK-015: "ddl-optin-seeding (2026-07-09): posture back to opt-in" + Decision "Mitigate (partial) + Accept residual" + review trigger -> Mitigated (partial) + Accepted residual (owner) [review trigger preserved]
RISK-016: "ddl-optin-seeding (2026-07-09): back to opt-in" + Decision "Accept" + review trigger -> Accepted (owner) [review trigger preserved]
RISK-017: "Accept (excluded)" + review trigger -> Accepted (excluded) [review trigger preserved]
RISK-018: "Mitigate ->" only (no status marker in row) -> Mitigated [FLAGGED]
RISK-019: "M1 status (m1-import-pipeline): now Mitigate (implemented) for all M1 path-construction surfaces" -> Mitigated (m1-import-pipeline)
RISK-020: "Mitigated (m8-auth-core); Residual closed (m8-rate-audit, v0.9.0)" + accepted _reauth residual + review trigger -> Mitigated (m8-auth-core; residual closed m8-rate-audit v0.9.0) [residual + review trigger preserved]
RISK-021: "M2 status (m2-hardening-performance): implemented; Row closed for M1/M2; no residual" -> Mitigated (m2-hardening-performance)
RISK-022: "Mitigated (m8-auth-core): FRG-SEC-005 implemented; Gap G-5 closed" -> Mitigated (m8-auth-core)
RISK-023: "implemented (m1-ui-opds-deploy)" -> Mitigated (m1-ui-opds-deploy)
RISK-024: "M2 status (m2-manual-import): single hardened site generalized; residual CBL import (B)" -> Mitigated (m1-search-indexers; extended m2-manual-import) [CBL-backlog residual preserved]
RISK-025: "M3 status (m3-pull-backbone): pull-source arm CLOSED; only remaining arm is SAB local_service; DNS-rebinding residual unchanged" -> Mitigated (m1-foundation; extended m3-pull-backbone) [DNS-rebinding accepted residual preserved]
RISK-026: "implemented (m1-downloads)" -> Mitigated (m1-downloads)
RISK-027: "M1 status (m1-downloads): DDL arm now CLOSED" -> Mitigated (m1-search-indexers; m1-downloads)
RISK-028: "implemented as designed (m1-downloads)" + Decision "Mitigate (by exclusion)" -> Mitigated (by exclusion) (m1-downloads)
RISK-029: "M1 status (m1-import-pipeline): Row closed for M1" -> Mitigated (m1-downloads; m1-import-pipeline)
RISK-030: "implemented on both download paths (m1-downloads)" -> Mitigated (m1-downloads)
RISK-031: "Accept (excluded)" + review trigger -> Accepted (excluded) [review trigger preserved]
RISK-032: "M1 status (m1-import-pipeline): atomicity cluster all fixed + regression-tested" -> Mitigated (m1-import-pipeline)
RISK-033: "Mitigate ->" only (no status marker in row) -> Mitigated [FLAGGED]
RISK-034: "Accept (excluded)" + review trigger -> Accepted (excluded) [review trigger preserved]
RISK-035: "M1 status (m1-search-indexers): moot for CV (JSON-only client)" -> Mitigated (m1-search-indexers)
RISK-036: "implemented (m1-foundation)" -> Mitigated (m1-foundation)
RISK-037: "Mitigate ->" only (no status marker; CBL import is backlog per RISK-024) -> Mitigated [FLAGGED]
RISK-038: "Mitigate ->" only (no status marker in row) -> Mitigated [FLAGGED]
RISK-039: "Posture change (pull-enabled-default, 2026-07-11): default true" + "M3 mitigation realised" + "M4 Trojan-Source stripping added" -> Mitigated (m3-pull-backbone; extended m4-pull-experience) [display-spoof residual preserved]
RISK-040: "Mitigate (m2-daily-surfaces): blocked-event dedup" -> Mitigated (m2-daily-surfaces)
RISK-041: "Mitigated (m6-keystore): at-rest secret encryption; weak-passphrase residual" (was Accept M2–M4) -> Mitigated (was Accepted M2–M4; m6-keystore) [weak-passphrase residual + review trigger preserved]
RISK-042: "Accept per KA-001 (owner decision 2026-07-09)" + review trigger -> Accepted (owner, 2026-07-09; KA-001) [review trigger preserved]
RISK-043: "M8 status (m8-auth-core): no-auth exposure lineage (RISK-020) closed; redaction-before-buffer remains" -> Mitigated (m4-logs-viewer; no-auth lineage closed m8-auth-core) [register_secret review trigger preserved]
RISK-044: "M8 status (m8-auth-core): no-auth exposure lineage (RISK-020) closed; validation-scoped display-only remains" -> Mitigated (m4-series-detail; no-auth lineage closed m8-auth-core)
RISK-045: "Mitigate (implemented) (m6-humble-source)" -> Mitigated (implemented) (m6-humble-source)
RISK-046: "Accept (residual, after mitigations) (m6-humble-source)" + review trigger -> Accepted (residual; m6-humble-source) [review trigger preserved]
RISK-047: "Mitigate (implemented) (m6-humble-source)" -> Mitigated (implemented) (m6-humble-source)
RISK-048: "Status: implemented in this change (m6-humble-source), FRG-SRC-006" + Decision "Mitigate" -> Mitigated (m6-humble-source)

## FLAGGED — rows whose current state was ambiguous, and how resolved

- **RISK-008 — resolved to Open (not Mitigated).** The row's latest markers say "stays OPEN ...
  FRG-DDL-015 is backlog B and unimplemented ... Not claimed as mitigated." This CONTRADICTS the
  register's own `## Counts` section, which lists RISK-008 inside the "RISK-001–014" Mitigate span.
  I followed the row text (Open), and added a parenthetical note in `## Counts` recording the
  discrepancy. The risk is dormant only because no extraction path exists yet; the mitigation
  (safe archive extraction) is a design commitment for backlog B. Owner should confirm the Counts
  span was always meant to exclude RISK-008.

- **RISK-004, RISK-009, RISK-018, RISK-033, RISK-037, RISK-038 — recorded Mitigated on the
  register's own accounting; not independently re-verified.** Each of these rows carries ONLY the
  original "Mitigate ->" requirement mapping with NO per-row implementation ("M1/M2/... status:
  implemented") marker, so whether the control is actually built is not evidenced within the row.
  The register's `## Counts` lists all six under Mitigate, so I recorded Status = Mitigated and
  appended "(Row carries only the original requirement mapping; no per-row implementation marker
  — see compaction log.)" to each mitigation cell rather than upgrade the claim silently.
  RISK-037 is the sharpest of these: RISK-024's row explicitly calls CBL reading-list import
  "backlog B", so RISK-037's hardened-parser mitigation is a design commitment for an unbuilt
  feature — its cell says so. Owner should confirm implementation status for these six (esp. 004
  = OPDS KDF/rate-limit/audit primitives, 009 = FlareSolverr handling, 037 = CBL import).

## Notes on preserved caveats (non-ambiguous, but load-bearing)

- Accepted residuals kept verbatim-ish in the mitigation cell: RISK-005 (zipfile ZipInfo
  materialisation), RISK-010 (RAR symlink/encryption + magic-only CBR), RISK-020 (`_reauth_admin`
  path not limiter-covered), RISK-025 (DNS-rebinding TOCTOU), RISK-041 (weak passphrase),
  RISK-014 (DDL scraped-text arm open), RISK-024 (CBL import backlog), RISK-039 (display-spoof).
- Review triggers preserved for all Accepted rows (015, 016, 017, 031, 034, 042, 046) and for
  Mitigated rows that still state a live trigger (020, 041, 043).
- Header prose rewritten: dropped the pre-M1 draft-requirement framing ("Read-only analysis
  staged outside the repo", "governing draft requirement", "sec-nfr-requirements.md",
  "LD=library-domain ..." draft-file legend). Kept the FRG-PROC-006 title, the L/M/H legend, and
  the Source-key explanation (all research-finding keys — opds/ddl/cv/fs/sonarr/filename-parsing
  + analysis — still appear in the Source column). Added the Status vocabulary and the
  git-log-reconstructs-history line.
- The `## Counts` and `## Canonical ownership note` sections were left substantially intact (out
  of the stated edit scope: header prose + table). One parenthetical note added to `## Counts`
  flagging the RISK-008 Open/Mitigate discrepancy.
