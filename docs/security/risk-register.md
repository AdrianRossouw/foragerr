# foragerr — Security Risk Register (FRG-PROC-006)

Read-only analysis staged outside the repo per FRG-PROC-008. One row per risk. `L/M/H` =
likelihood / impact. **Decision**: *mitigate* cites the governing draft requirement (by its
`### AREA — <name>` heading, which the completeness-critic audit confirmed resolve 1:1 to the
now-allocated FRG IDs) or a NEW requirement in
`sec-nfr-requirements.md` (prefixed `SEC-new:`); *accept* gives rationale + a review trigger.
"Source": research finding (`opds S1`, `ddl §4`, `cv §4`, `fs §N`) or `analysis`.

Every security flag in the research docs is represented (traceability column notes which).
Draft files: LD=library-domain, ACQ=acquisition, FD=files-domain, IF=interfaces, PF=platform.

| ID | Description | STRIDE | Component | L | I | Decision | Source |
|----|-------------|--------|-----------|---|---|----------|--------|
| RISK-001 | Arbitrary file read via client-supplied path to an OPDS download/stream endpoint (Mylar `deliverFile` traversal — the headline finding). | Info disclosure | OPDS | H | H | **Mitigate** → IF `OPDS — Library-id-based file resolution only (no client-supplied paths)` (impossible by construction; FRG-PROC-006 STRIDE entry). | opds S1 |
| RISK-002 | SQL injection via client-controlled ids/params interpolated into SQL (Mylar `_Comic`/`_StoryArc` string concat). | Tampering | OPDS/DB/API | M | H | **Mitigate** → IF `OPDS — Parameterized queries throughout` + IF `API — Paging envelope` (whitelisted sort keys for ORDER BY). | opds S3 |
| RISK-003 | OPDS world-readable and exempt from site auth by default. | Spoofing/Elevation | OPDS | H | M | **Mitigate** → PF `AUTH — HTTP Basic for OPDS realm` + `AUTH — uniform coverage of all surfaces` (M3); pre-M3 folds into RISK-020. | opds S2 |
| RISK-004 | Weak OPDS auth primitives: plaintext-compared Basic, no TLS enforced, no lockout/rate limit. | Spoofing/MITM | OPDS | M | M | **Mitigate** → PF `AUTH — password storage with modern KDF`, `AUTH — login rate limiting and audit`; TLS via PF `DEP — Tailscale-scoped exposure`. | opds S4 |
| RISK-005 | Zip-bomb / decompression / truncated-image resource exhaustion when the server opens untrusted archives and resizes images (PIL `LOAD_TRUNCATED_IMAGES`) at OPDS feed/stream time. | DoS | OPDS/Archive | M | M | **Mitigate** → IF `OPDS — Resource limits on archive and image handling` + `OPDS — Cached page counts…` + `OPDS — Acquisition feeds…` (no archive I/O at feed time). | opds S5 |
| RISK-006 | Download filename derived from attacker-controlled redirect-final URL (`unquote` before `basename`); traversal / hostile / device names. | Tampering/Path traversal | DDL | M | H | **Mitigate** → ACQ `DDL — Safe filename generation` (system-generated names; FRG-PROC-006 entry). | ddl §4 |
| RISK-007 | Scraped hrefs fetched with session cookies and redirects followed blindly (SSRF to internal addresses; cookie exfiltration; `run.php`/`go.php` URL mutation). | SSRF/Info disclosure | DDL | M | H | **Mitigate** → ACQ `DDL — Outbound URL security` (per-provider scheme/host allowlist, redirect cap re-validated, no cross-host cookies) + PF `NFR — bounded, verified outbound requests`. | ddl §4 |
| RISK-008 | `zipfile.extractall` on hostile packs: zip-bombs, `../` traversal entries, symlink tricks; extraction before verification. | DoS/Tampering | DDL/Archive | M | H | **Mitigate** → ACQ `DDL — Safe archive extraction` (entry/size caps, traversal+symlink rejection, staging dir). Also see RISK-010 for non-DDL paths. | ddl §4 |
| RISK-009 | FlareSolverr call uses `verify=False`; Cloudflare clearance cookies persisted in plaintext (`.gc_cookies.dat`); solver URL is an SSRF pivot. | MITM/Credential exposure | DDL | M | M | **Mitigate** → ACQ `DDL — Cloudflare session handling` (TLS-verified, 0600 cookies as credential, never logged/exported) + `DDL — Outbound URL security` (no `verify=False` anywhere). | ddl §4 |
| RISK-010 | Decompression-bomb / zip-slip / malformed-image exposure on the **import**, **cover-extraction**, and **ComicInfo-tagging** archive paths (DDL/OPDS limits do not cover these). | DoS/Tampering | Archive | M | M | **Mitigate** → SEC-new: `Archive-processing safety (cross-cutting)`. Drafts flag but do not formalize (IF OPDS Notes "may hoist to cross-area"). | analysis (extends opds S5 / ddl §4) |
| RISK-011 | User-editable ComicVine wiki HTML flows into names/aliases/descriptions → stored XSS in UI **and** steers downstream indexer/DDL search queries. | Injection/Steering | ComicVine/API | M | M | **Mitigate** → LD `META — ComicVine content is untrusted input` + PF `NFR — untrusted external content handling`. | cv §4 |
| RISK-012 | `CV_VERIFY` disables TLS certificate verification globally for ComicVine traffic. | Tampering/MITM | ComicVine | L | M | **Mitigate** → LD `META — ComicVine client fundamentals` (TLS on, no global off-knob) + PF `NFR — bounded, verified outbound requests`. | cv §4 |
| RISK-013 | Secret exposure: CV API key in URL query strings; indexer/SAB keys in logs; Mylar's reversible salted-base64 at-rest obfuscation; secrets in image/repo/diagnostics. | Info disclosure | Secrets/Config/DB | M | H | **Mitigate** → LD `META — API key handling`, PF `NFR — secret redaction in logs and errors`, PF `AUTH — at-rest secret encryption`, PF `DEP — secrets never in image or repository`, PF `DEP — secrets-stripped diagnostic bundle`. | cv §4; fs §7/§8; CLAUDE.md |
| RISK-014 | Scraped/remote text (DDL series titles/sizes/years, CV fields) written unescaped into logs, DB, UI → XSS + log injection. | Tampering/XSS/Log-inject | DDL/API | M | M | **Mitigate** → PF `NFR — untrusted external content handling` + LD `META — …untrusted input`. Residual CR/LF log forging → see FRG-NFR-014 (listener request resource limits) Notes / structured-logging (PF `DEP — structured logging`). | ddl §4; cv §4 |
| RISK-015 | Single hardcoded DDL upstream (getcomics.org) → domain takeover becomes a malware-delivery channel; CRC does not authenticate content. | Spoofing/Tampering | DDL | L | H | **Mitigate (partial) + Accept residual** → ACQ `DDL — Content verification before import` + FD `PP — Archive validity verification` (structural checks). Deep malware/AV scanning **accepted** for a single-user private tool. **Review trigger**: if DDL is ever exposed beyond the operator, or a malware incident occurs. | ddl §4 |
| RISK-016 | Cloudflare evasion + spoofed User-Agent + shortener filtering are ToS-sensitive automation choices. | Repudiation/ToS | DDL | M | L | **Accept** → conscious, registry-recorded operational decision (ACQ `DDL — Cloudflare session handling` Notes). Rationale: personal-use tool; no misrepresentation of a person. **Review trigger**: GetComics ToS change or distribution of the tool. | ddl §4 |
| RISK-017 | Mylar's per-provider proxy applied only to the DDL session (split-tunnel surprise; other traffic ignores the proxy). | Tampering/Info disclosure | DDL | L | L | **Accept (excluded)** → per-provider proxy not carried over; if proxy is ever added it is process-wide (ACQ `DDL — Outbound URL security` Notes). **Review trigger**: proxy support requested. | ddl §4 |
| RISK-018 | Hostile filename crashes the parser (Mylar wraps whole passes in bare `except`; no fuzzing). | DoS | Parser | L | M | **Mitigate** → FD `IMP — Structured parse result…no crashes` (fuzz-tested, never raises) + `IMP — Pure, deterministic parse function`. | filename-parsing §4 (analysis) |
| RISK-019 | Attacker-influenced ComicVine series/issue titles used to build destination folder/file paths could escape the managed library root (`..`, separators, absolute paths). | Tampering/Path traversal | Parser/File mover | M | H | **Mitigate** → SEC-new: `Filesystem path confinement`. Partial today via LD `META — …untrusted input` (sanitized folder names) + FD `PP — Token-based renaming engine` (illegal-char policy), but no central safe-join/containment guarantee. | analysis |
| RISK-020 | **M1/M2 ship with NO application auth** on UI/API/OPDS/WebSocket; only compensating control is Tailscale-only exposure. | Spoofing/Elevation | Whole listener / Infra | H | H | **Accept (owner-approved), M3 mitigate** → **canonical owner: PF `AUTH — M1/M2 no-auth accepted risk`**; PF `DEP — Tailscale-scoped exposure` is the compensating-control statement, NOT a second accept. Auth lands via PF `AUTH — single-user web login` + `AUTH — uniform coverage of all surfaces` (M3). **Review trigger**: any exposure beyond the tailnet, or before M3 sign-off. | fs §8; CLAUDE.md |
| RISK-021 | No listener-level request-body-size cap or request rate limit → memory/CPU DoS from large or rapid requests (incl. WebSocket floods). | DoS | API/WS | M | M | **Mitigate** → FRG-NFR-014 (listener request resource limits). | analysis |
| RISK-022 | Cross-Site WebSocket Hijacking (unvalidated Origin) and unconsolidated CSRF stance on state-changing endpoints once cookie-session auth exists. | Spoofing/Elevation | WS/API | L | M | **Mitigate** → SEC-new: `CSRF & WebSocket-origin` (complements PF `AUTH — session management`). Milestone M3. | analysis |
| RISK-023 | Hotlinked ComicVine cover URLs leak iPad/OPDS client requests to a third-party CDN (traffic-analysis / availability coupling). | Info disclosure | OPDS | L | L | **Mitigate** → IF `OPDS — Cover and thumbnail links with local fallback` (served locally, no client egress). | opds §2 |
| RISK-024 | XXE / entity-expansion (billion-laughs) via untrusted XML: Newznab/Torznab RSS responses and any residual CV/OPDS XML; only CBL got a hardening note. | DoS/Info disclosure | Indexer/ComicVine/OPDS | M | M | **Mitigate** → SEC-new: `Hardened XML parsing`. Partial: LD `META — client fundamentals` moves CV to JSON; ARC `CBL reading-list import` Notes require a hardened parser — but the indexer RSS/XML parser is uncovered. | cv §4 (expat); analysis (Newznab XML) |
| RISK-025 | SSRF via config-/wiki-supplied outbound URLs: ComicVine cover-image fetch (wiki-editable `ImageURL`), and indexer/SAB/FlareSolverr/pull-source hosts pointed at internal/loopback addresses. DDL allowlist does not cover these. | SSRF | ComicVine/Indexer/SAB/Pull | M | M | **Mitigate** → SEC-new: `SSRF egress controls`. Partial: PF `NFR — bounded, verified outbound requests` (timeouts/redirect caps only). | analysis (extends ddl §4 SSRF) |
| RISK-026 | Indexer returns a mislabelled payload (HTML/error page where an NZB is expected) → bad content into the pipeline. | Spoofing/Tampering | Indexer | L | M | **Mitigate** → ACQ `DL — SABnzbd add via file upload` (fetch+validate NZB before handoff) + ACQ `IDX — Newznab response parsing and error mapping`. | analysis (sonarr §4.2) |
| RISK-027 | Hostile/slow provider (indexer or GetComics) wedges workers or triggers a ban; unbounded pagination walk. | DoS | Indexer/DDL | M | M | **Mitigate** → PF `NFR — bounded, verified outbound requests`, `NFR — indexer and DDL politeness with failure backoff`, ACQ `IDX — Indexer failure back-off and recovery`, ACQ `DDL — Politeness and provider self-protection`. | ddl §1.8/§3.7; fs §3 |
| RISK-028 | Mylar's SAB add-by-URL scheme (SAB pulls the NZB from Mylar's API via a one-time download key) adds a callback auth surface. | Elevation/Info disclosure | SABnzbd | L | M | **Mitigate (by exclusion)** → ACQ `DL — SABnzbd add via file upload` (FRG-DL-003: server-side upload; add-by-URL + download-key permanently excluded) + FRG-DL-009 (CDH-only intake; external completion scripts excluded in its Notes). | fs §5; sonarr §4.2 |
| RISK-029 | SAB-reported completed path (remote/foreign) read/moved by the process → path confusion / reading unintended locations. | Tampering | SABnzbd/PP | L | M | **Mitigate** → ACQ `DL — Remote path mapping` + FD `PP — Remote path mapping` (mapped, validated, import-blocked-not-crash) + SEC-new `Filesystem path confinement`. | fs §4; sonarr §4.2/4.5 |
| RISK-030 | Password-protected / corrupt archives consume resources or slip into the library. | Tampering/DoS | Archive/DL | L | M | **Mitigate** → FD `PP — Archive validity verification` + ACQ `DL — Failed download handling` (encrypted → failed). | fs §4; sonarr §4.6 |
| RISK-031 | Shell script hooks (pre/extra/on-snatch) are a remote-code-execution surface (Mylar feature). | Elevation | PP/File mover | L | H | **Accept (excluded)** → FD `PP — Permissions and ownership enforcement` Notes: hooks omitted entirely (single-user tool, no payoff). **Review trigger**: any request to reintroduce user scripts. | fs §4 |
| RISK-032 | Crash/power-loss loses acknowledged work or corrupts queue state → duplicate snatches / double imports (Mylar smears state across globals/DB/memory). | Tampering/DoS | Scheduler/Queues | M | M | **Mitigate** → PF `SCHED — persisted command queue surviving restart`, `SCHED — command de-duplication`, `NFR — crash-safe queues and idempotent work`. | ddl §3.2; sonarr §4.3-4.5 |
| RISK-033 | Container runs privileged / library volumes owned by root → host-level blast radius. | Elevation | Docker/Infra | L | M | **Mitigate** → PF `DEP — Docker image per linuxserver.io conventions` (PUID/PGID, non-root) + `DEP — all persistent state under /config`. | fs §8; CLAUDE.md |
| RISK-034 | Self-update mechanism (git pull / tarball fetch) is a supply-chain/RCE surface (Mylar feature). | Tampering/Elevation | Infra | L | H | **Accept (excluded)** → PF `DEP — no self-update` (upgrade only by image redeploy). **Review trigger**: any request for in-app updates. | fs §8 |
| RISK-035 | ComicVine XML parsing via minidom/expat (entity-expansion unsafe in some configs). | DoS | ComicVine | L | M | **Mitigate** → LD `META — ComicVine client fundamentals` (JSON, not XML) — folds into RISK-024 for any residual XML. | cv §4 |
| RISK-036 | ComicVine `requests.get` without a timeout hangs the scheduler thread indefinitely. | DoS | ComicVine | M | M | **Mitigate** → PF `NFR — bounded, verified outbound requests` + LD `META — ComicVine client fundamentals` (mandatory timeouts). | cv §4 |
| RISK-037 | CBL reading-list import parses untrusted XML from the internet (XXE/entity expansion). | DoS/Info disclosure | Import/Archive | L | M | **Mitigate** → LD (library-domain) `ARC — CBL reading-list import` Notes (hardened parser, no entity expansion) + SEC-new `Hardened XML parsing`. | fs §2; analysis |
| RISK-038 | Spoofed browser User-Agent to ComicVine (ToS/politeness inheritance). | Repudiation/ToS | ComicVine | L | L | **Mitigate** → LD `META — ComicVine client fundamentals` (honest configurable UA — diverges from Mylar). | cv §4 |
| RISK-039 | External weekly-pull JSON source (unofficial third party) as an outbound dependency and untrusted-content ingress. | Info disclosure/Tampering | Pull source | L | L | **Mitigate** → LD `PULL — External pull-source fetch` (timeouts, error-code handling, degraded-health) + PF `NFR — untrusted external content handling` + SEC-new `SSRF egress controls`. | fs §1 |

## Counts

- **Total risks**: 39
- **Mitigate**: 33 (RISK-001–014, 018–030, 032, 033, 035–039) — several with a partial/residual
  note; RISK-015 is mitigate-partial + accept-residual and is counted once under mitigate.
- **Accept**: 6 — RISK-016 (DDL ToS evasion), RISK-017 (per-provider proxy excluded), RISK-020
  (**the M1 no-auth accepted risk**), RISK-031 (script hooks excluded), RISK-034 (self-update
  excluded), plus RISK-015's residual (malware/AV depth) recorded within its mitigate row.
  (If RISK-015 residual is tallied separately: 33 mitigate / 6 accept + 1 partial-accept.)

## Canonical ownership note (as required)

The **M1 no-auth accepted risk** is stated in two drafts:
- PF `AUTH — M1/M2 no-auth accepted risk`, and
- PF `DEP — Tailscale-scoped exposure`.

**Designated canonical owner: PF `AUTH — M1/M2 no-auth accepted risk`** (RISK-020). The DEP entry
is the *compensating-control* statement (network-scoped exposure) and must reference the AUTH
requirement rather than record a second, independent acceptance. The orchestrator should allocate
one accepted-risk ID owned by AUTH and have DEP cite it.
