# foragerr — Security Risk Register (FRG-PROC-006)

Read-only analysis staged outside the repo per FRG-PROC-008. One row per risk. `L/M/H` =
likelihood / impact. **Decision**: *mitigate* cites the governing draft requirement (by its
`### AREA — <name>` heading, which the completeness-critic audit confirmed resolve 1:1 to the
now-allocated FRG IDs) or a NEW requirement in
`sec-nfr-requirements.md` (prefixed `SEC-new:`); *accept* gives rationale + a review trigger.
"Source": research finding (`opds S1`, `ddl §4`, `cv §4`, `fs §N`) or `analysis`.

Every security flag in the research docs is represented (traceability column notes which).
Draft files: LD=library-domain, ACQ=acquisition, FD=files-domain, IF=interfaces, PF=platform.

| ID | Description | STRIDE | Component | L | I | Decision | Source |
|----|-------------|--------|-----------|---|---|----------|--------|
| RISK-001 | Arbitrary file read via client-supplied path to an OPDS download/stream endpoint (Mylar `deliverFile` traversal — the headline finding). | Info disclosure | OPDS | H | H | **Mitigate** → IF `OPDS — Library-id-based file resolution only (no client-supplied paths)` (impossible by construction; FRG-PROC-006 STRIDE entry). | opds S1 |
| RISK-002 | SQL injection via client-controlled ids/params interpolated into SQL (Mylar `_Comic`/`_StoryArc` string concat). | Tampering | OPDS/DB/API | M | H | **Mitigate** → IF `OPDS — Parameterized queries throughout` + IF `API — Paging envelope` (whitelisted sort keys for ORDER BY). | opds S3 |
| RISK-003 | OPDS world-readable and exempt from site auth by default. | Spoofing/Elevation | OPDS | H | M | **Mitigate** → PF `AUTH — HTTP Basic for OPDS realm` + `AUTH — uniform coverage of all surfaces` (M3); pre-M3 folds into RISK-020. | opds S2 |
| RISK-004 | Weak OPDS auth primitives: plaintext-compared Basic, no TLS enforced, no lockout/rate limit. | Spoofing/MITM | OPDS | M | M | **Mitigate** → PF `AUTH — password storage with modern KDF`, `AUTH — login rate limiting and audit`; TLS via PF `DEP — Tailscale-scoped exposure`. | opds S4 |
| RISK-005 | Zip-bomb / decompression / truncated-image resource exhaustion when the server opens untrusted archives and resizes images (PIL `LOAD_TRUNCATED_IMAGES`) at OPDS feed/stream time. | DoS | OPDS/Archive | M | M | **Mitigate** → IF `OPDS — Resource limits on archive and image handling` + `OPDS — Cached page counts…` + `OPDS — Acquisition feeds…` (no archive I/O at feed time). **M1 status (m1-import-pipeline)**: the shared limits utility this row's OPDS mitigation will reuse now exists — `security.archives.inspect_archive` enforces member-count / per-member / total *declared* decompressed-size caps from the central directory before a single byte is decompressed (FRG-SEC-003). OPDS page streaming itself is M3 (`opds-page-streaming`), which must adopt this utility rather than opening archives its own way; the PIL image-resize arm stays with that change. | opds S5 |
| RISK-006 | Download filename derived from attacker-controlled redirect-final URL (`unquote` before `basename`); traversal / hostile / device names. | Tampering/Path traversal | DDL | M | H | **Mitigate** → ACQ `DDL — Safe filename generation` (system-generated names; FRG-PROC-006 entry). **M1 status (m1-downloads)**: implemented — `safe_output_name` (FRG-DDL-011) builds `{series} {issue} [__{issueid}__]{ext}` from library metadata + queue id, each component reduced by the shared `safe_path_component` (FRG-NFR-012); no redirect-final URL or `Content-Disposition` value ever reaches the path (static-tested against a hostile-CD response + traversal-name corpus), and `resolve_output_path` is a containment backstop that raises if a name resolves outside the staging dir. The extension comes from verified magic bytes, never the remote name. | ddl §4 |
| RISK-007 | Scraped hrefs fetched with session cookies and redirects followed blindly (SSRF to internal addresses; cookie exfiltration; `run.php`/`go.php` URL mutation). | SSRF/Info disclosure | DDL | M | H | **Mitigate** → ACQ `DDL — Outbound URL security` (per-provider scheme/host allowlist, redirect cap re-validated, no cross-host cookies) + PF `NFR — bounded, verified outbound requests`. **M1 status (m1-downloads)**: implemented — a per-provider scheme(`https`)+host `AllowList` (FRG-DDL-012) is enforced by a `hop_check` re-run on every redirect hop, on top of the always-on `external` SSRF egress policy (FRG-SEC-001), with no cross-host cookies. This change extended the gate — previously on the file download only — to the scraped post-page AND search-page fetches (`build_hop_check` used by `search_provider` + `queue`), closing the residual where a hostile GetComics response could steer a page fetch to an arbitrary public host. | ddl §4 |
| RISK-008 | `zipfile.extractall` on hostile packs: zip-bombs, `../` traversal entries, symlink tricks; extraction before verification. | DoS/Tampering | DDL/Archive | M | H | **Mitigate** → ACQ `DDL — Safe archive extraction` (entry/size caps, traversal+symlink rejection, staging dir). Also see RISK-010 for non-DDL paths. **M1 status (m1-downloads)**: stays OPEN — safe archive extraction (FRG-DDL-015) is backlog B and unimplemented; M1 DDL lands single files only (`verify_file` opens a `.cbz` read-only with NO extraction, FRG-DDL-010), so no `extractall` ever runs on the DDL path yet. Not claimed as mitigated. **M1 status (m1-import-pipeline)**: still no extraction anywhere in the codebase, but the gate a future extractor must pass now exists and is honest — `ArchiveReport.safe_to_extract` is `True` only when every member was enumerated from the central directory AND passed the zip-slip/symlink/nesting/size rules; it is DISTINCT from `ok` (which means "passed the M1 import validity check" and is `True` for a magic-only cbr/cb7 whose members were never vetted). FRG-DDL-015 must gate on `safe_to_extract`, never `ok`. | ddl §4 |
| RISK-009 | FlareSolverr call uses `verify=False`; Cloudflare clearance cookies persisted in plaintext (`.gc_cookies.dat`); solver URL is an SSRF pivot. | MITM/Credential exposure | DDL | M | M | **Mitigate** → ACQ `DDL — Cloudflare session handling` (TLS-verified, 0600 cookies as credential, never logged/exported) + `DDL — Outbound URL security` (no `verify=False` anywhere). | ddl §4 |
| RISK-010 | Decompression-bomb / zip-slip / malformed-image exposure on the **import**, **cover-extraction**, and **ComicInfo-tagging** archive paths (DDL/OPDS limits do not cover these). | DoS/Tampering | Archive | M | M | **Mitigate** → SEC-new: `Archive-processing safety (cross-cutting)`. Drafts flag but do not formalize (IF OPDS Notes "may hoist to cross-area"). **M1 status (m1-import-pipeline)**: implemented for the **import** arm — `security.archives.inspect_archive` (FRG-SEC-003) is the single shared entry point the pipeline's archive-valid decision calls: central-directory-only caps (member count, per-member and total declared decompressed size, nesting depth 0 = no archive-in-archive), rejection of absolute / drive-qualified / `..`-escaping / backslash-normalized member names (zip-slip family), symlink-entry rejection, encrypted-flag rejection, ≥1-image rule for cbz; it never extracts and never raises on hostile input (typed `ArchiveReport` rejections; hostile corpus committed as fixtures). **Documented latents, all M1-safe because nothing extracts**: (a) `ok=True` on a magic-only cbr/cb7 means "passed the import validity gate", NOT "members vetted" — the honest `listed`/`safe_to_extract` flags exist precisely so no later consumer over-reads `ok` (see RISK-008); (b) the RAR listing path (when `rarfile` imports) applies the name/size/nesting/count rules but does NOT check symlink members or RAR encryption flags — and with `rarfile` absent, a CBR passes on magic alone (accepted M1 residual, design decision 4). Cover-extraction (M2/M3) and ComicInfo-tagging (M2) arms must route through this same utility when they land. | analysis (extends opds S5 / ddl §4) |
| RISK-011 | User-editable ComicVine wiki HTML flows into names/aliases/descriptions → stored XSS in UI **and** steers downstream indexer/DDL search queries. | Injection/Steering | ComicVine/API | M | M | **Mitigate** → LD `META — ComicVine content is untrusted input` + PF `NFR — untrusted external content handling`. **M1 status (m1-library-metadata)**: implemented for the ComicVine ingest arm — `sanitize_cv_text()` (FRG-META-014) strips HTML/collapses whitespace/caps length on every CV string before it leaves the metadata client, so no raw wiki HTML reaches the DB, API responses, or downstream search-query construction (FRG-NFR-012 tagged tests). Non-CV steering arms (indexer/DDL text) remain open, tracked for changes 4/5. | cv §4 |
| RISK-012 | `CV_VERIFY` disables TLS certificate verification globally for ComicVine traffic. | Tampering/MITM | ComicVine | L | M | **Mitigate** → LD `META — ComicVine client fundamentals` (TLS on, no global off-knob) + PF `NFR — bounded, verified outbound requests`. **M1 status (m1-foundation)**: implemented — the shared HTTP factory enforces TLS verify with no opt-out parameter (FRG-NFR-006 tagged tests). | cv §4 |
| RISK-013 | Secret exposure: CV API key in URL query strings; indexer/SAB keys in logs; Mylar's reversible salted-base64 at-rest obfuscation; secrets in image/repo/diagnostics. | Info disclosure | Secrets/Config/DB | M | H | **Mitigate** → LD `META — API key handling`, PF `NFR — secret redaction in logs and errors`, PF `AUTH — at-rest secret encryption`, PF `DEP — secrets never in image or repository`, PF `DEP — secrets-stripped diagnostic bundle`. **M1 status (m1-foundation)**: log/error redaction implemented (FRG-NFR-008) with secrets self-registering at config load; repo/image hygiene per FRG-DEP-005. At-rest encryption remains M3 (FRG-AUTH-008). **M1 status (m1-downloads)**: the SAB API key is a `SecretStr`, `register_secret`-registered for log redaction at client construction (FRG-NFR-008); it rides as an `apikey` query param to SAB (inherent to the SAB protocol) but is scrubbed from our logs/errors. At-rest encryption of the stored `download_clients` row still lands M3 (FRG-AUTH-008). | cv §4; fs §7/§8; CLAUDE.md |
| RISK-014 | Scraped/remote text (DDL series titles/sizes/years, CV fields) written unescaped into logs, DB, UI → XSS + log injection. | Tampering/XSS/Log-inject | DDL/API | M | M | **Mitigate** → PF `NFR — untrusted external content handling` + LD `META — …untrusted input`. Residual CR/LF log forging → see FRG-NFR-014 (listener request resource limits) Notes / structured-logging (PF `DEP — structured logging`). **M1 status (m1-library-metadata)**: implemented for the ComicVine arm — every CV-sourced string (series/issue names, descriptions, publisher) is sanitized at ingest by `sanitize_cv_text()` (FRG-META-014/FRG-NFR-012) before it can reach logs, DB, or the API; control characters (incl. CR/LF) are stripped, closing the log-forging vector for CV text specifically. The DDL text arm remains open (change 5). **M1 status (m1-downloads)**: the DDL text arm remains OPEN, not advanced — scraped GetComics titles/sizes/years become `ReleaseCandidate` fields treated as untrusted text end-to-end (same posture as indexer release titles; output encoding is the UI's job). No ingest-time HTML/CR-LF sanitizer equivalent to `sanitize_cv_text` is applied to DDL text. Separately, the failed-release loop is hardened: grab rows + the blocklist now carry `pub_date` (FRG-DL-012), so the multi-field match key actually distinguishes a resurfacing known-bad post. | ddl §4; cv §4 |
| RISK-015 | Single hardcoded DDL upstream (getcomics.org) → domain takeover becomes a malware-delivery channel; CRC does not authenticate content. | Spoofing/Tampering | DDL | L | H | **Mitigate (partial) + Accept residual** → ACQ `DDL — Content verification before import` + FD `PP — Archive validity verification` (structural checks). Deep malware/AV scanning **accepted** for a single-user private tool. **Review trigger**: if DDL is ever exposed beyond the operator, or a malware incident occurs. | ddl §4 |
| RISK-016 | Cloudflare evasion + spoofed User-Agent + shortener filtering are ToS-sensitive automation choices. | Repudiation/ToS | DDL | M | L | **Accept** → conscious, registry-recorded operational decision (ACQ `DDL — Cloudflare session handling` Notes). Rationale: personal-use tool; no misrepresentation of a person. **Review trigger**: GetComics ToS change or distribution of the tool. | ddl §4 |
| RISK-017 | Mylar's per-provider proxy applied only to the DDL session (split-tunnel surprise; other traffic ignores the proxy). | Tampering/Info disclosure | DDL | L | L | **Accept (excluded)** → per-provider proxy not carried over; if proxy is ever added it is process-wide (ACQ `DDL — Outbound URL security` Notes). **Review trigger**: proxy support requested. | ddl §4 |
| RISK-018 | Hostile filename crashes the parser (Mylar wraps whole passes in bare `except`; no fuzzing). | DoS | Parser | L | M | **Mitigate** → FD `IMP — Structured parse result…no crashes` (fuzz-tested, never raises) + `IMP — Pure, deterministic parse function`. | filename-parsing §4 (analysis) |
| RISK-019 | Attacker-influenced ComicVine series/issue titles used to build destination folder/file paths could escape the managed library root (`..`, separators, absolute paths). | Tampering/Path traversal | Parser/File mover | M | H | **Mitigate** → SEC-new: `Filesystem path confinement`. Partial today via LD `META — …untrusted input` (sanitized folder names) + FD `PP — Token-based renaming engine` (illegal-char policy), but no central safe-join/containment guarantee. **M1 status (m1-library-metadata)**: partially implemented — `safe_path_component()` (FRG-NFR-012) strips path separators/control chars/reserved device names/trailing dots-spaces from every CV title before it becomes a folder-name *segment*, so an embedded `../` sequence cannot introduce a real directory boundary; `validate_under_root()` (FRG-SER-008) rejects any per-series path override that resolves outside a registered root folder. Still open (deferred to change 6's renaming engine, tracked as FRG-SEC-004): a central safe-join/containment guarantee across the full destination-path construction pipeline (symlink escape, root-resolution TOCTOU) — this row stays **Mitigate (partial)** until then. **M1 status (m1-import-pipeline)**: the deferred central guarantee is now implemented — `security.paths.safe_join(root, *parts)` (FRG-SEC-004) is the ONLY sanctioned constructor for import/renamer destination paths: every untrusted part is reduced to a single segment via `safe_path_component` (which relocated into `security.paths` so ONE module owns path safety — no second copy exists, enforced by scenario test), and the assembled path is realpath-resolved and confinement-checked against the root, which also catches escape through a pre-existing symlink in the tree; any escape raises `PathConfinementError`. The renamer renders folder templates into *segments* handed to `safe_join`; the pipeline builds its destination exclusively through it. This row is now **Mitigate (implemented)** for all M1 path-construction surfaces. | analysis |
| RISK-020 | **M1/M2 ship with NO application auth** on UI/API/OPDS/WebSocket; only compensating control is Tailscale-only exposure. | Spoofing/Elevation | Whole listener / Infra | H | H | **Accept (owner-approved), M3 mitigate** → **canonical owner: PF `AUTH — M1/M2 no-auth accepted risk`**; PF `DEP — Tailscale-scoped exposure` is the compensating-control statement, NOT a second accept. Auth lands via PF `AUTH — single-user web login` + `AUTH — uniform coverage of all surfaces` (M3). **Review trigger**: any exposure beyond the tailnet, or before M3 sign-off. **M1 status (m1-foundation)**: the listener now exists; acceptance restated — auth mode none is verified by route-inventory tests (FRG-AUTH-001), Tailscale-only exposure (FRG-DEP-011) documented as the sole compensating control. | fs §8; CLAUDE.md |
| RISK-021 | No listener-level request-body-size cap or request rate limit → memory/CPU DoS from large or rapid requests (incl. WebSocket floods). | DoS | API/WS | M | M | **Mitigate** → FRG-NFR-014 (listener request resource limits). | analysis |
| RISK-022 | Cross-Site WebSocket Hijacking (unvalidated Origin) and unconsolidated CSRF stance on state-changing endpoints once cookie-session auth exists. | Spoofing/Elevation | WS/API | L | M | **Mitigate** → SEC-new: `CSRF & WebSocket-origin` (complements PF `AUTH — session management`). Milestone M3. | analysis |
| RISK-023 | Hotlinked ComicVine cover URLs leak iPad/OPDS client requests to a third-party CDN (traffic-analysis / availability coupling). | Info disclosure | OPDS | L | L | **Mitigate** → IF `OPDS — Cover and thumbnail links with local fallback` (served locally, no client egress). | opds §2 |
| RISK-024 | XXE / entity-expansion (billion-laughs) via untrusted XML: Newznab/Torznab RSS responses and any residual CV/OPDS XML; only CBL got a hardening note. | DoS/Info disclosure | Indexer/ComicVine/OPDS | M | M | **Mitigate** → SEC-new: `Hardened XML parsing`. Partial: LD `META — client fundamentals` moves CV to JSON; ARC `CBL reading-list import` Notes require a hardened parser — but the indexer RSS/XML parser is uncovered. **M1 status (m1-search-indexers)**: implemented for the indexer arm (FRG-SEC-002) — single defusedxml parse site (DTD/external entities/expansion disabled, byte-capped), hostile-corpus tagged tests (billion-laughs, XXE, quadratic, oversized, junk) + static no-other-parser guard. Residual: CBL import (B) must reuse the same hardened site. | cv §4 (expat); analysis (Newznab XML) |
| RISK-025 | SSRF via config-/wiki-supplied outbound URLs: ComicVine cover-image fetch (wiki-editable `ImageURL`), and indexer/SAB/FlareSolverr/pull-source hosts pointed at internal/loopback addresses. DDL allowlist does not cover these. | SSRF | ComicVine/Indexer/SAB/Pull | M | M | **Mitigate** → SEC-new: `SSRF egress controls`. Partial: PF `NFR — bounded, verified outbound requests` (timeouts/redirect caps only). **M1 status (m1-foundation)**: egress controls implemented at the shared factory (FRG-SEC-001): per-hop scheme/DNS validation refusing loopback/link-local/private/ULA, external vs local-service profiles, hostile-fixture tested. **Accepted residual**: DNS-rebinding TOCTOU (policy resolve vs connect-time re-resolve window); compensating controls are per-hop re-validation and disabled auto-redirects; review if the threat model leaves home-server scope. **M1 status (m1-downloads)**: SAB arm handled by egress-profile split (FRG-SEC-001) — SAB API calls use the `local_service` profile bound to the operator-configured base URL (deliberately permits the LAN/loopback SAB host the strict `external` profile would refuse; compensating control = base URL is operator-configured, not attacker-supplied), while the remote NZB fetch uses the strict `external` profile. The DDL scraped-fetch arm is additionally confined by the per-provider host allowlist (see RISK-007). Pull-source arm still open. | analysis (extends ddl §4 SSRF) |
| RISK-026 | Indexer returns a mislabelled payload (HTML/error page where an NZB is expected) → bad content into the pipeline. | Spoofing/Tampering | Indexer | L | M | **Mitigate** → ACQ `DL — SABnzbd add via file upload` (fetch+validate NZB before handoff) + ACQ `IDX — Newznab response parsing and error mapping`. **M1 status (m1-downloads)**: implemented — the SAB client fetches the NZB itself over the `external` profile and runs `_validate_nzb` BEFORE upload (FRG-DL-003): non-empty, parses under the ONE hardened `parse_indexer_xml` defusedxml site (reused from FRG-SEC-002), and contains ≥1 `<segment>`. A hostile/mislabelled/empty payload is a typed `GrabValidationError` and is never POSTed to SAB. | analysis (sonarr §4.2) |
| RISK-027 | Hostile/slow provider (indexer or GetComics) wedges workers or triggers a ban; unbounded pagination walk. | DoS | Indexer/DDL | M | M | **Mitigate** → PF `NFR — bounded, verified outbound requests`, `NFR — indexer and DDL politeness with failure backoff`, ACQ `IDX — Indexer failure back-off and recovery`, ACQ `DDL — Politeness and provider self-protection`. **M1 status (m1-search-indexers)**: implemented for indexers — persisted back-off ladder + per-indexer 2s spacing (FRG-NFR-005), paging hard cap, per-indexer fan isolation wedge-proven end-to-end against hang/drip/junk/429 fixtures (FRG-NFR-010); the generic (provider_type, provider_id) ladder is ready for DL/DDL reuse in change 5. **M1 status (m1-downloads)**: DDL arm now CLOSED — GetComics page fetches are spaced ≥15 s (floor-clamped) plus jitter with the per-provider last-run persisted across restart (FRG-DDL-006), pagination is depth-capped, and 429/503, a Cloudflare challenge marker, or a connection fault fast-forward the shared `PROVIDER_DDL` back-off ladder (FRG-NFR-005); the SAB client escalates a `PROVIDER_DOWNLOAD_CLIENT` ladder on unreachable/non-200/non-JSON responses. | ddl §1.8/§3.7; fs §3 |
| RISK-028 | Mylar's SAB add-by-URL scheme (SAB pulls the NZB from Mylar's API via a one-time download key) adds a callback auth surface. | Elevation/Info disclosure | SABnzbd | L | M | **Mitigate (by exclusion)** → ACQ `DL — SABnzbd add via file upload` (FRG-DL-003: server-side upload; add-by-URL + download-key permanently excluded) + FRG-DL-009 (CDH-only intake; external completion scripts excluded in its Notes). **M1 status (m1-downloads)**: implemented as designed — intake is `mode=addfile` only (multipart NZB upload after the server-side fetch+validate); no add-by-URL path and no one-time download-key callback exists in `SabnzbdClient`. Indexer credentials never reach SAB. | fs §5; sonarr §4.2 |
| RISK-029 | SAB-reported completed path (remote/foreign) read/moved by the process → path confusion / reading unintended locations. | Tampering | SABnzbd/PP | L | M | **Mitigate** → ACQ `DL — Remote path mapping` + FD `PP — Remote path mapping` (mapped, validated, import-blocked-not-crash) + SEC-new `Filesystem path confinement`. **M1 status (m1-downloads)**: implemented — `apply_mappings` rewrites a completed `storage` path through the client's per-host `RemotePathMapping` prefixes (FRG-DL-005); a foreign, unmapped path (Windows-shaped on this POSIX host, or any path when mappings exist yet none match) is surfaced as a `WARNING` item carrying "check remote path mapping" rather than a silent import failure — pure/deterministic, never probes the filesystem, never crashes. The central safe-join/containment guarantee at the import step (FRG-SEC-004) still lands with change 6's mover. **M1 status (m1-import-pipeline)**: that promise is fulfilled — the import mover constructs its destination exclusively via `safe_join` under the series' library root, so even a mapped-but-hostile completed path cannot steer a write outside the managed root; the source side is only ever *read* (verified, then moved with `place_file`'s copy-verify-delete discipline). An unmapped/foreign path still surfaces as the change-5 mapping warning and blocks rather than crashes. Row closed for M1. | fs §4; sonarr §4.2/4.5 |
| RISK-030 | Password-protected / corrupt archives consume resources or slip into the library. | Tampering/DoS | Archive/DL | L | M | **Mitigate** → FD `PP — Archive validity verification` + ACQ `DL — Failed download handling` (encrypted → failed). **M1 status (m1-downloads)**: implemented on both download paths — SAB history items whose name is `ENCRYPTED/`-prefixed or whose fail-message contains a password fragment map to `FAILED` (+ `encrypted` flag + reason) (FRG-DL-004); on the DDL path `verify_file` rejects a corrupt/non-container file (magic-byte type check + a `.cbz` must open as a real zip with ≥1 image, size floor) before import, failing the download (FRG-DDL-010). | fs §4; sonarr §4.6 |
| RISK-031 | Shell script hooks (pre/extra/on-snatch) are a remote-code-execution surface (Mylar feature). | Elevation | PP/File mover | L | H | **Accept (excluded)** → FD `PP — Permissions and ownership enforcement` Notes: hooks omitted entirely (single-user tool, no payoff). **Review trigger**: any request to reintroduce user scripts. | fs §4 |
| RISK-032 | Crash/power-loss loses acknowledged work or corrupts queue state → duplicate snatches / double imports (Mylar smears state across globals/DB/memory). | Tampering/DoS | Scheduler/Queues | M | M | **Mitigate** → PF `SCHED — persisted command queue surviving restart`, `SCHED — command de-duplication`, `NFR — crash-safe queues and idempotent work`. **M1 status (m1-import-pipeline)**: the import execution arm is hardened against exactly this class (the gate review's atomicity cluster, all fixed + regression-tested): (1) the drain claims a row with a status-guarded atomic `UPDATE … WHERE state IN (import_pending, importing)`, so two workers can never process the same item; (2) `execute()` performs the irreversible on-disk move FIRST and only then quarantines the superseded file and swaps the `issue_files` row — a failure can no longer roll back the DB while the file is already gone; (3) each candidate runs in its own SAVEPOINT with escaping filesystem errors converted to a BLOCKED outcome, so one candidate cannot poison a sibling's committed row; (4) crash recovery reconciles FS↔DB before deciding — a file already moved into the library (identified by its `[__issueid__]` tag) by a run whose transaction rolled back is *adopted* rather than orphaned; (5) drain and rescan share a file-mutation exclusivity group, so double-import safety no longer rests on the worker pool being size 1; (6) a manual queue remove refuses (409) an actively-importing item and de-tracks before telling the client to delete data; (7) one row's failure is isolated and retried next cycle instead of aborting the whole batch (FRG-DL-009/010, FRG-PP-002/007, FRG-SER-010). | ddl §3.2; sonarr §4.3-4.5 |
| RISK-033 | Container runs privileged / library volumes owned by root → host-level blast radius. | Elevation | Docker/Infra | L | M | **Mitigate** → PF `DEP — Docker image per linuxserver.io conventions` (PUID/PGID, non-root) + `DEP — all persistent state under /config`. | fs §8; CLAUDE.md |
| RISK-034 | Self-update mechanism (git pull / tarball fetch) is a supply-chain/RCE surface (Mylar feature). | Tampering/Elevation | Infra | L | H | **Accept (excluded)** → PF `DEP — no self-update` (upgrade only by image redeploy). **Review trigger**: any request for in-app updates. | fs §8 |
| RISK-035 | ComicVine XML parsing via minidom/expat (entity-expansion unsafe in some configs). | DoS | ComicVine | L | M | **Mitigate** → LD `META — ComicVine client fundamentals` (JSON, not XML) — folds into RISK-024 for any residual XML. **M1 status (m1-search-indexers)**: moot for CV (JSON-only client since change 3, no XML path exists); all in-process XML parsing now goes through the single FRG-SEC-002 hardened site per RISK-024. | cv §4 |
| RISK-036 | ComicVine `requests.get` without a timeout hangs the scheduler thread indefinitely. | DoS | ComicVine | M | M | **Mitigate** → PF `NFR — bounded, verified outbound requests` + LD `META — ComicVine client fundamentals` (mandatory timeouts). **M1 status (m1-foundation)**: implemented — mandatory connect/read/write/pool timeouts on every factory client; hung-server abort tagged-tested (FRG-NFR-006). | cv §4 |
| RISK-037 | CBL reading-list import parses untrusted XML from the internet (XXE/entity expansion). | DoS/Info disclosure | Import/Archive | L | M | **Mitigate** → LD (library-domain) `ARC — CBL reading-list import` Notes (hardened parser, no entity expansion) + SEC-new `Hardened XML parsing`. | fs §2; analysis |
| RISK-038 | Spoofed browser User-Agent to ComicVine (ToS/politeness inheritance). | Repudiation/ToS | ComicVine | L | L | **Mitigate** → LD `META — ComicVine client fundamentals` (honest configurable UA — diverges from Mylar). | cv §4 |
| RISK-039 | External weekly-pull JSON source (unofficial third party) as an outbound dependency and untrusted-content ingress. | Info disclosure/Tampering | Pull source | L | L | **Mitigate** → LD `PULL — External pull-source fetch` (timeouts, error-code handling, degraded-health) + PF `NFR — untrusted external content handling` + SEC-new `SSRF egress controls`. | fs §1 |
| RISK-040 | Unbounded `import_history` growth from a blocked-item retry loop: a still-completed `import_blocked` item is re-fed to `import_pending` by every ~1-min tracking cycle (the deliberate retry-on-evidence-change path), and each failed retry writes a fresh `import_blocked` history event — a permanently stuck item ping-pongs and accretes ~1.4k rows/day until the user acts. | DoS (disk/DB, slow) | PP/DB | M | L | **Accept (documented latent, found at the change-6 gate)** — single-user tool; growth is slow, bounded per item per cycle, and the stuck item is loudly visible in the queue with its blocking reasons, which is the designed prompt for user action. **Review trigger**: M2 history UI / housekeeping change — add blocked-event dedup (don't rewrite an identical reasons-set) or history pruning there. | analysis (change-6 gate review) |

## Counts

- **Total risks**: 40
- **Mitigate**: 33 (RISK-001–014, 018–030, 032, 033, 035–039) — several with a partial/residual
  note; RISK-015 is mitigate-partial + accept-residual and is counted once under mitigate.
- **Accept**: 7 — RISK-016 (DDL ToS evasion), RISK-017 (per-provider proxy excluded), RISK-020
  (**the M1 no-auth accepted risk**), RISK-031 (script hooks excluded), RISK-034 (self-update
  excluded), RISK-040 (blocked-item history-growth latent, review at M2), plus RISK-015's
  residual (malware/AV depth) recorded within its mitigate row.
  (If RISK-015 residual is tallied separately: 33 mitigate / 7 accept + 1 partial-accept.)

## Canonical ownership note (as required)

The **M1 no-auth accepted risk** is stated in two drafts:
- PF `AUTH — M1/M2 no-auth accepted risk`, and
- PF `DEP — Tailscale-scoped exposure`.

**Designated canonical owner: PF `AUTH — M1/M2 no-auth accepted risk`** (RISK-020). The DEP entry
is the *compensating-control* statement (network-scoped exposure) and must reference the AUTH
requirement rather than record a second, independent acceptance. The orchestrator should allocate
one accepted-risk ID owned by AUTH and have DEP cite it.
